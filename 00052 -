using System;
public class Kata
{
  public static int PositiveSum(int[] arr)
  {
    int result = 0;
    for (int i = 0; i < arr.Length; i++)
    {
      if (Math.Abs(arr[i]) == arr[i]) result+=arr[i]; 
    }
    
    return result;
  }
}
