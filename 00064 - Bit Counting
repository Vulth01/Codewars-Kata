using System;

public class Kata
{
  public static int CountBits(int n)
  {
    string binary = Convert.ToString(n, 2);
    int i = 0;
    
    foreach (char c in binary){
      if (c == '1') i++;
    }
    
    return i;
  }
}
